# TalkCaddy Environment Configuration
# Copy this file to .env and fill in your values
# Never commit .env to version control!

# Backend API URL (development)
BACKEND_URL=http://localhost:8888

# Backend API URL (production)
BACKEND_URL_PROD=https://talkcaddy.com/api

# Google Sign-In: Web Client ID from Google Cloud Console
# (APIs & Services > Credentials > OAuth 2.0 Client IDs > Web application)
GOOGLE_SERVER_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Sentry DSN for error reporting (production only)
# Get it from https://sentry.io > Project Settings > Client Keys (DSN)
SENTRY_DSN=https://your-key@your-org.ingest.sentry.io/your-project-id

# Sentry Auth Token for uploading debug symbols (required for readable stack traces)
# Generate at: https://sentry.io/settings/auth-tokens/
# Required scopes: project:releases, org:read
SENTRY_AUTH_TOKEN=your-sentry-auth-token

# Mixpanel Token for analytics
# Get it from https://mixpanel.com > Project Settings
MIXPANEL_TOKEN=your-mixpanel-token

# ChatGPT Custom GPT ID for TalkCaddy voice caddie
# This is the ID from your custom GPT URL: https://chatgpt.com/g/{GPT_ID}
GPT_ID=g-694fb86cbfc88191a75ebc68f14565b3-talkcaddy-v1

# ChatGPT Custom GPT ID (production)
GPT_ID_PROD=g-694fb86cbfc88191a75ebc68f14565b3-talkcaddy-v1
